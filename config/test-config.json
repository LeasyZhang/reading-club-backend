{
    "db": {
        "url" : "host=localhost port=5432 user=joe.zhang dbname=demo sslmode=disable",
        "dbEngine" : "postgres",
        "maxIdleConnections": 10,
        "maxOpenConnections": 20
    },

    "jwt" : {
        "timeout" : 10,
        "maxRefresh" : 30
    }
    
}