{
    "db": {
        "url" : "host=full_postgres port=5432 user=joe dbname=demo password=password sslmode=disable",
        "dbEngine" : "postgres",
        "maxIdleConnections": 10,
        "maxOpenConnections": 20
    },

    "jwt" : {
        "timeout" : 180,
        "maxRefresh" : 86400
    }
    
}